name: Lemery policy inheritance

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  company-policy:
    uses: BuiltByLegacy/Lemery-Organization/.github/workflows/reusable-lemery-policy.yml@main
